import { Meta } from '@storybook/blocks'

<Meta title="Design Tokens/Elevation" />

# Elevation

List of elevation tokens.

| Token | Treatment | Usage |
| --- | --- | --- |
| Flat | `border border-border` | Cards, tables, inputs |
| Raised | `shadow-sm` | Default and destructive buttons |
| Overlay | `shadow-lg` + `bg-black/40` backdrop | Modal, popover |

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, minmax(0, 1fr))', gap: 20, margin: '24px 0' }}>
  <div style={{ background: 'var(--card)', color: 'var(--card-foreground)', border: '1px solid var(--border)', borderRadius: 'var(--radius)', padding: 16 }}>
    <div style={{ fontSize: 13, fontWeight: 600 }}>Flat</div>
    <div style={{ fontSize: 12, color: 'var(--muted-foreground)', marginTop: 4 }}>border only</div>
  </div>
  <div style={{ background: 'var(--card)', color: 'var(--card-foreground)', border: '1px solid var(--border)', borderRadius: 'var(--radius)', padding: 16, boxShadow: '0 1px 2px 0 rgb(0 0 0 / 0.05)' }}>
    <div style={{ fontSize: 13, fontWeight: 600 }}>Raised</div>
    <div style={{ fontSize: 12, color: 'var(--muted-foreground)', marginTop: 4 }}>shadow-sm</div>
  </div>
  <div style={{ background: 'var(--card)', color: 'var(--card-foreground)', border: '1px solid var(--border)', borderRadius: 'var(--radius)', padding: 16, boxShadow: '0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)' }}>
    <div style={{ fontSize: 13, fontWeight: 600 }}>Overlay</div>
    <div style={{ fontSize: 12, color: 'var(--muted-foreground)', marginTop: 4 }}>shadow-lg</div>
  </div>
</div>
