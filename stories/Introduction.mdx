import { Meta } from '@storybook/blocks'

<Meta title="Getting Started/Introduction" />

# Lavender Design System

A themed, accessible component library powering [Lavender Finance](https://lavender-finance.vercel.app). 24+ production-ready React components with a full design token system, dual-theme support, and comprehensive Storybook documentation.

---

## System Architecture

```
Design Tokens (CSS custom properties)
  ├── Semantic tokens (background, foreground, primary, border…)
  ├── Chart tokens (chart-1 through chart-5)
  ├── Extended palettes (lavenderDawn.*, lavenderMoon.*)
  └── Scale tokens (radius, spacing via Tailwind)
        │
        ▼
Tailwind Config (maps tokens → utilities)
        │
        ▼
Primitives & Utilities
  ├── cn() — class composition (clsx + tailwind-merge)
  ├── ThemeProvider — context + localStorage persistence
  └── ThemeSwitcher — toggle component
        │
        ▼
UI Components
  ├── Inputs:     Button, Dropdown, Autocomplete, Combobox, PinCode, StarRating
  ├── Layout:     Card, Modal, Accordion, Tabs, Tooltip
  ├── Data:       TransactionList, VirtualizedList, StatCard, Chart
  ├── Feedback:   Badge, ProgressBar, Loading, ErrorDisplay, Skeleton
  └── Forms:      CreditCardForm, PassportForm
        │
        ▼
Product (Lavender Finance)
```

---

## Token Layers

The system uses three token layers that allow components to work across themes without any conditional logic:

| Layer | Example | Purpose |
|-------|---------|---------|
| **Semantic** | `--primary`, `--border`, `--muted` | Theme-aware values that swap automatically between Dawn and Moon |
| **Chart** | `--chart-1` through `--chart-5` | Data visualization colors tuned for contrast in both themes |
| **Extended** | `lavenderDawn.iris`, `lavenderMoon.foam` | Fixed-hex values for when a specific color is needed regardless of theme |

→ See **Getting Started / Design Tokens** for the full palette and setup guide.

---

## Component Inventory

| Category | Components | Count |
|----------|-----------|-------|
| **Input** | Button, Dropdown, Autocomplete, Combobox, PinCode, StarRating | 6 |
| **Layout** | Card, Modal, Accordion, Tabs, Tooltip | 5 |
| **Data Display** | TransactionList, VirtualizedList, StatCard, Chart (Area/Bar/Line) | 4 |
| **Feedback** | Badge, ProgressBar, Loading, ErrorDisplay, Skeleton, Pagination | 6 |
| **Forms** | CreditCardForm, PassportForm | 2 |
| **Theme** | ThemeProvider, ThemeSwitcher | 2 |

---

## Design Decisions

### Accessibility First
- All interactive components support full keyboard navigation
- Focus rings use `--ring` token for theme consistency
- ARIA attributes on compound components (Accordion, Tabs, Modal, Tooltip)
- Color contrast ratios meet WCAG AA in both themes

### Composable Architecture
- Each component is a self-contained directory (`ComponentName.tsx`, `ComponentName.types.ts`, `index.ts`)
- Variant logic separated into `*.styles.ts` files using `cva`-style patterns
- `cn()` utility enables safe class overrides without specificity wars

### Theme System
- CSS custom properties on `:root` / `.dark` — zero JavaScript runtime cost for theming
- `ThemeProvider` context for programmatic control with `localStorage` persistence
- Storybook's `withThemeByClassName` decorator for live theme switching in docs

---

## Quick Start

```bash
npm install clsx tailwind-merge lucide-react tailwindcss-animate
```

1. Copy the component directory into your project
2. Add `cn()` utility and theme tokens (see **Tokens / Getting Started**)
3. Wrap your app in `ThemeProvider`
4. Use components — they respond to the active theme automatically
